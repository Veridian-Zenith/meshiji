import 'package:flutter/material.dart';

class AppTheme {
  // Essential colors - Black & Gold
  static const Color gold = Color(0xFFFFD700);
  static const Color goldAccent = Color(0xFFFFFF00);
  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF1A1A1A);
  static const Color textColor = Color(0xFFFFD700);

  // Basic sizing
  static const double sidebarWidth = 100.0;
  static const double toolbarHeight = 40.0;
  static const double fileTileHeight = 50.0;
  static const double spacing = 8.0;
}
